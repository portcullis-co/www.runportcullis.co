---
import { SignedIn, SignIn,  SignedOut, UserButton, SignInButton } from "@clerk/astro/components";
import MainLayout from "@/layouts/main-layout.astro";
import { cn } from "@/lib/utils";
---

<MainLayout title="Auth" description="Authenticate with your Portcullis client account">
  <header>
    <SignedIn>
      <UserButton />
    </SignedIn>
    <SignedOut>
      <SignIn />
    </SignedOut>
  </header>
  <slot />
</MainLayout>